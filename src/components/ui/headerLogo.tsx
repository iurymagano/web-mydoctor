import { Card } from "./card";

const HeaderLogo = () => {
  return (
    <Card className="border-none shadow-none">
      <h1 className="mx-auto my-0 max-w-6xl px-6 text-[32px] font-bold text-primary max-sm:text-xl ">
        MYDOCTOR
      </h1>
    </Card>
  );
};

export default HeaderLogo;
